import Settings from "../components/Settings";

export const metadata = {
  title: "सेटिंग्ज | Settings - आरती संग्रह",
  description: "Configure theme, reading size, script, and preferences.",
};

export default function SettingsPage() {
  return <Settings />;
}
